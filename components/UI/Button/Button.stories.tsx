import Button from './Button';

const Story = {
    title: 'Button',
    component: Button,
}

const Template = () => <Button>Hello Button</Button>

export const Default = Template.bind({});

export default Story;